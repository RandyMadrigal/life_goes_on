{
  "name": "life-goes-on",
  "private": true,
  "scripts": {
    "dev": "npm run --prefix src/apps/web/src dev",
    "dev:api": "npm run --prefix src/apps/api dev",
    "dev:all": "npm run dev:api & npm run dev",
    "build": "npm run build:web && npm run build:api",
    "build:web": "npm run --prefix src/apps/web/src build",
    "build:api": "npm run --prefix src/apps/api build",
    "preview": "npm run --prefix src/apps/web/src preview",
    "install:all": "npm install --prefix src/apps/web/src && npm install --prefix src/apps/api"
  }
}
