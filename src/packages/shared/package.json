{
  "name": "life-goes-on-shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript contracts (DTOs, API envelope) between the API and the web app. Type-only — no runtime code, no build step required.",
  "main": "./src/index.ts",
  "types": "./src/index.ts"
}
